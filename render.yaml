databases:
  - name: agent-onsen-db
    plan: free
    databaseName: agent_onsen
    user: agent_onsen

services:
  - type: web
    name: agent-onsen
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: agent-onsen-db
          property: connectionString
      - key: PORT
        value: "8000"
